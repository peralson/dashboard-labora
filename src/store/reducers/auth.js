import {} from "../actions/auth";

const initialState = {
  token: "kbhjsacfkjuhwloqlaqnqelovfqvclobq",
};

// eslint-disable-next-line import/no-anonymous-default-export
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
